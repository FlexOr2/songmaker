<!--
	The root library address needs no address resolution and no overlay of its
	own — it renders nothing. `(library)/+layout.svelte` mounts LibraryWorkspace
	for all three library addresses (issue #276); this page exists only so `/`
	is a route inside that group.
-->
