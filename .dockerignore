# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# ACE-Step source tree is COPYed into the acestep-base image at /opt/acestep so
# the inner subprocess venv can be baked at build time. Only host-runtime
# artifacts and the heavy weights are excluded; weights are bind-mounted at
# runtime by the acestep-worker leaf service.
vendor/acestep/.venv/
vendor/acestep/.git/
vendor/acestep/.cache/
vendor/acestep/checkpoints/
vendor/acestep/gradio_outputs/
vendor/acestep/manual_uv_sync.log
vendor/acestep/acestep-*.stderr.log
vendor/acestep/acestep_stderr.log

# Frontend dev artifacts
frontend/node_modules/
frontend/.svelte-kit/

# Data
_output/
data/

# Git
.git/
.gitignore

# IDE
.idea/
.vscode/

# Plans and docs (not needed in image)
plans/
docs/

# Environment (secrets)
.server.env
.session_secret

# Tests (not needed in image)
tests/

# Docker
docker-compose.yml
Dockerfile
.dockerignore
